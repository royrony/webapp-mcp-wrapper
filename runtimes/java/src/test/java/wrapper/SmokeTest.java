package wrapper;

import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Test;

/** JUnit 5 smoke test verifying the test harness is wired up. */
class SmokeTest {
  @Test
  void harnessRuns() {
    assertTrue(true);
  }
}
